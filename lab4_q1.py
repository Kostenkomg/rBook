"""
Лабораторная работа № 4: Абстракция данных и списки
====================================================

Вопрос 1. Доступ к элементам вложенных списков.

В Python списки могут содержать другие списки (вложенные списки).
Чтобы добраться до элемента внутри вложенного списка, нужно использовать
цепочку индексов: сначала индекс внешнего списка, затем индекс внутреннего
и так далее.

Пример:
    x = [1, 3, [5, 7], 9]
    x[2]      -> [5, 7]        (внешний индекс 2)
    x[2][1]   -> 7             (внутренний индекс 1)

Если индекс выходит за пределы списка, возникает ошибка IndexError.
"""


# ============================================================
# Вопрос 1
# ============================================================
def _q_01():
    """
    Часть 1. Найти индексы, при которых выражение возвращает 7.

    >>> x = [1, 3, [5, 7], 9]                                   # doctest: +SKIP
    >>> x[2][1]                                                 # doctest: +SKIP
    7

    Пояснение:
        x[2]    -> [5, 7]
        x[2][1] -> 7

    >>> x = [[7]]                                               # doctest: +SKIP
    >>> x[0][0]                                                 # doctest: +SKIP
    7

    Пояснение:
        x[0]    -> [7]
        x[0][0] -> 7

    >>> x = [3, 2, 1, [9, 8, 7]]                                # doctest: +SKIP
    >>> x[3][2]                                                 # doctest: +SKIP
    7

    Пояснение:
        x[3]    -> [9, 8, 7]
        x[3][2] -> 7

    >>> x = [[3, [5, 7], 9]]                                    # doctest: +SKIP
    >>> x[0][1][1]                                              # doctest: +SKIP
    7

    Пояснение:
        x[0]       -> [3, [5, 7], 9]
        x[0][1]    -> [5, 7]
        x[0][1][1] -> 7


    Часть 2. Определить результат выражений.

    >>> lst = [3, 2, 7, [84, 83, 82]]                           # doctest: +SKIP
    >>> lst[4]                                                  # doctest: +SKIP
    IndexError

    Пояснение:
        Список lst содержит 4 элемента с индексами 0, 1, 2, 3.
        Индекс 4 выходит за пределы списка -> IndexError:
        "list index out of range".

    >>> lst[3][0]                                               # doctest: +SKIP
    84

    Пояснение:
        lst[3]    -> [84, 83, 82]
        lst[3][0] -> 84
    """
    return 0


# ============================================================
# Демонстрация работы (необязательная часть)
# ============================================================
def main():
    print("=== Часть 1: получение элемента 7 ===")

    x = [1, 3, [5, 7], 9]
    print(f"x = {x}")
    print(f"x[2][1] = {x[2][1]}\n")

    x = [[7]]
    print(f"x = {x}")
    print(f"x[0][0] = {x[0][0]}\n")

    x = [3, 2, 1, [9, 8, 7]]
    print(f"x = {x}")
    print(f"x[3][2] = {x[3][2]}\n")

    x = [[3, [5, 7], 9]]
    print(f"x = {x}")
    print(f"x[0][1][1] = {x[0][1][1]}\n")

    print("=== Часть 2: проверка индексов ===")

    lst = [3, 2, 7, [84, 83, 82]]
    print(f"lst = {lst}")

    try:
        print(f"lst[4] = {lst[4]}")
    except IndexError as e:
        print(f"lst[4] -> IndexError: {e}")

    print(f"lst[3][0] = {lst[3][0]}")


if __name__ == "__main__":
    main()
